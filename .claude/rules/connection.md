---
paths:
  - "cmd/musterd/**"
  - "internal/server/server*.go"
  - "internal/server/auth*.go"
  - "internal/server/ws*.go"
  - "internal/server/state*.go"
  - "internal/server/staticserve_test.go"
  - "internal/server/fakes_test.go"
  - "internal/server/helpers_test.go"
  - "internal/webui/**"
  - "web/src/features/connection.ts"
  - "web/src/ws*.ts"
  - "web/src/protocol*.ts"
  - "web/src/api*.ts"
  - "web/src/app*.ts"
  - "web/src/dom.ts"
  - "web/src/main.ts"
  - "web/src/render/banner*.ts"
  - "web/e2e/auth.spec.ts"
  - "web/e2e/resilience.spec.ts"
  - "web/e2e/claude-version.spec.ts"
  - "web/e2e/embedded.spec.ts"
  - "web/e2e/type-scale.spec.ts"
  - "web/e2e/helpers/daemon.ts"
  - "web/e2e/helpers/db.ts"
  - "web/e2e/helpers/fixtures.ts"
---
<!-- kb:generated 1679cdb8ba0a1bf6 — regenerated by make gen-kb; edit the sources, not this file -->
# connection

Token and cookie auth, the /ws hello and snapshot, protocol version, connection banner, Claude version readout.

Read first: `docs/features/connection/spec.md`, `docs/features/connection/contract.md`

- `kb:adr/connection-banner-only-after-first-hello` — The daemon-down banner appears only after a first successful hello; a fresh page load shows a connecting state instead. (accepted, 2026-08-22; features: connection) → `docs/adr/connection-banner-only-after-first-hello.md`
- `kb:adr/connection-built-assets-ignored-with-gitkeep` — Built dashboard assets are gitignored; a committed placeholder keeps the embed directive compiling on a fresh clone, and the web build restores it. (accepted, 2026-08-31; features: connection) → `docs/adr/connection-built-assets-ignored-with-gitkeep.md`
- `kb:adr/connection-commands-http-ws-push-only` — Commands go over HTTP; the state WebSocket is server-to-client only, and the only client-to-server socket traffic is terminal input and resize. (accepted, 2026-08-20; features: connection) → `docs/adr/connection-commands-http-ws-push-only.md`
- `kb:adr/connection-dashboard-auto-opens-on-terminal` — musterd opens the dashboard in the browser at startup by default, only when stdin is a real terminal; that condition, not the flag, keeps tests browser-free. (accepted, 2026-08-31; features: connection) → `docs/adr/connection-dashboard-auto-opens-on-terminal.md`
- `kb:adr/connection-dashboard-embedded-in-binary` — The built dashboard is embedded in musterd and served by default; the disk directory flag remains as a development override with unchanged behaviour. (accepted, 2026-08-31; features: connection) → `docs/adr/connection-dashboard-embedded-in-binary.md`
- `kb:adr/connection-installed-claude-classified-never-refused` — The installed Claude Code is classified unknown, below, verified or above; the daemon serves identically in all four and the masthead shows a glyph, no dismiss. (accepted, 2026-09-10; features: connection, canary) → `docs/adr/connection-installed-claude-classified-never-refused.md`
- `kb:adr/connection-missing-web-build-fails-fast` — A musterd built with no dashboard and no disk override exits non-zero at startup naming both remedies, replacing the old silent-404 mode. (accepted, 2026-08-31; features: connection) → `docs/adr/connection-missing-web-build-fails-fast.md`
- `kb:adr/connection-protocol-bumps-only-on-shape-change` — The protocol version bumps only when an existing field changes shape; additive messages and fields do not bump, and a skewed open tab is told to reload. (accepted, 2026-09-10; features: connection) → `docs/adr/connection-protocol-bumps-only-on-shape-change.md`
- `kb:adr/connection-ui-token-reusable-not-one-time` — The UI token is reusable at the auth endpoint for the install's life; one-time described the launcher flow, not token burning. (accepted, 2026-08-22; features: connection) → `docs/adr/connection-ui-token-reusable-not-one-time.md`
- `kb:adr/connection-whole-object-session-upserts` — The state stream sends whole session objects on every change; the client replaces by id and sorts for display itself. (accepted, 2026-08-20; features: connection, lifecycle) → `docs/adr/connection-whole-object-session-upserts.md`
- `kb:adr/ingest-separate-token-in-url-path` — Two tokens: the UI token is exchanged for a strict same-site cookie, and a separate ingest token is embedded in the hook URL path. (accepted, 2026-08-20; features: ingest, connection) → `docs/adr/ingest-separate-token-in-url-path.md`
- `kb:adr/process-composition-roots-registration-only` — The web entry module and server constructor are composition roots only; each feature is a controller or handler type registered in one line, files not packages. (accepted, 2026-09-11; features: connection) → `docs/adr/process-composition-roots-registration-only.md`
- `kb:adr/release-drops-js-sourcemap` — Release builds omit the dashboard's JavaScript sourcemap; local and E2E builds keep it, so the shipped artifact differs from the tested one by the maps alone. (accepted, 2026-08-31; features: release, connection) → `docs/adr/release-drops-js-sourcemap.md`
- `kb:adr/stack-frontend-web-app-served-by-daemon` — The frontend is a daemon-served web app with interactive terminal panes in an app window; TUI, Gio, Fyne, Electron and a read-only side dashboard lost. (accepted, 2026-08-16; features: connection, surfaces) → `docs/adr/stack-frontend-web-app-served-by-daemon.md`
- `kb:adr/stack-http-stdlib-net-http` — The daemon's HTTP server is the standard library's net/http with method-and-path routing; no web framework. (accepted, 2026-08-16; features: connection) → `docs/adr/stack-http-stdlib-net-http.md`
- `kb:adr/stack-wails-desktop-shell-deferred` — A Wails desktop wrapper around the browser-served dashboard is deferred; the app window comes from the browser until a dock icon matters. (proposed, 2026-08-16; features: connection) → `docs/adr/stack-wails-desktop-shell-deferred.md`
- `kb:adr/stack-websocket-coder` — WebSockets use coder/websocket; the stdlib has none, x/net/websocket is deprecated, and Echo would wrap a third-party library anyway. (accepted, 2026-08-16; features: connection, surfaces) → `docs/adr/stack-websocket-coder.md`
- `kb:adr/surfaces-shell-spawn-http-then-attach-ws` — A shell is spawned lazily by an HTTP post and attached over a second socket that never spawns, so a spawn failure has a body the dashboard can render. (accepted, 2026-09-05; features: surfaces, connection) → `docs/adr/surfaces-shell-spawn-http-then-attach-ws.md`
- `kb:adr/surfaces-tmux-preflight-at-startup` — tmux is a hard dependency checked by a bounded preflight before the data dir or listener exists; a missing or broken tmux is fatal with the remedy on stderr. (accepted, 2026-08-31; features: surfaces, connection) → `docs/adr/surfaces-tmux-preflight-at-startup.md`
- `kb:adr/theme-banner-tokens-not-rose` — The daemon-down banner uses dedicated banner tokens; the rose token stays reserved for the Failed state. (accepted, 2026-08-22; features: theme, connection) → `docs/adr/theme-banner-tokens-not-rose.md`
- `kb:adr/theme-claude-theme-read-only-poll` — Claude Code's theme is read, never set: musterd polls one key of Claude's config file read-only and broadcasts a light, dark or unknown family. (accepted, 2026-09-02; features: theme, connection) → `docs/adr/theme-claude-theme-read-only-poll.md`
- `kb:fact/status-version-matches-installed` — The status-line version field equals the installed claude --version. (active, 2026-09-11; verified 2.1.233..2.1.267; features: canary, connection) → `docs/facts/status-version-matches-installed.md`
