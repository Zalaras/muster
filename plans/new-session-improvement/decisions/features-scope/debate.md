# Debate: features-scope

### Turn 1 — advocate-a (argument)

**The brief's premise for Option B is wrong, and I think that settles most of it.** Option B keeps `connection` out of the header "because `web/src/terminal/pane.ts` is owned by surfaces alone". But `kb for` (run just now) gives *five* files whose only owner is `connection`: `internal/server/server.go`, `web/src/api.ts`, `web/src/api.test.ts`, `web/src/main.ts`, `web/e2e/helpers/daemon.ts`. Launch's globs (`docs/features/launch/spec.md:9-11`) match none of them; connection's (`docs/features/connection/spec.md:9-11`: `internal/server/server*.go`, `web/src/api*.ts`, `web/src/main.ts`, `web/e2e/helpers/daemon.ts`) match all five. The brief's "every listed file (except `pane.ts`) is also matched by launch's own globs, *or is the file launch's behaviour goes through*" — the "or" covers exactly these five. Under B's any-owner rule with header `launch, focus, tiles, surfaces`, those five still fail. B as written does not turn the gate green; B-that-works is `+ surfaces, connection` plus the script change.

**Connection's records are the ones this plan needs.** The gate's stated purpose (`features-scope.sh:9-11`): the pack keys on the header, so a missing feature leaves reviewers "without that feature's records". This plan adds a *refusal* (`kb:adr/launch-refuses-model-outside-binary-catalog`) keyed on the installed Claude Code binary. Connection owns `kb:adr/connection-installed-claude-classified-never-refused` (features `[connection, canary]`) — "a refusal would strand a user whose Claude Code auto-updated overnight". `writeDecisions` includes an ADR only when its features intersect the header (`internal/kb/pack.go:188`), so today no reviewer sees the one accepted decision this plan's core behaviour must be reconciled against. And `server.go` gained a new constant `modelCheckTimeout` in connection's composition root (`git diff main...HEAD -- internal/server/server.go`) — a placement question for review-maintainability that needs connection's conventions.

**B also leaves doc-reconcile unreconciled.** `.claude/agents/doc-reconcile.md:41-44` blocks on *any* changed file mapping outside the header. B changes only the script; unless the agent definition changes too, this run blocks at Step 7 after review — precisely the frontmatter incident (`plans/frontmatter/doc-reconcile.cycle1.md:3-4`) the gate was built to prevent. That is a second out-of-plan edit to pipeline doctrine, in the gate's first multi-owner run, to make a failing gate pass.

**Cost of A.** +~11 000 words per review pack over the surfaces-only row (34 778 vs 23 716). The budget warns, never fails (kb:adr/process-size-linters-warn-never-fail), and every row is already over it. actions/rail/rename are, I grant, low-value for `sessions.go` — but A is the gate's own remedy, needs no doctrine change, and is the only option that is green as stated.

### Turn 1 — advocate-b (concession)

Steelman: Option B as written does not make the gate green. Five changed files (`server.go`, `api.ts`, `api.test.ts`, `main.ts`, `web/e2e/helpers/daemon.ts`) have `connection` as their only owner. Any version of B that works has to add `connection` too, and it has to change `doc-reconcile.md` Step 1 as well as the script. Meanwhile connection's records hold an accepted ADR that this plan's refusal must be reconciled against.

I checked each part. `kb for` gives `connection` as the only owner of those five files. `docs/features/connection/spec.md` globs `internal/server/server*.go`, `web/src/api*.ts`, `web/src/main.ts` and `web/e2e/helpers/daemon.ts`. So the brief's premise ("add only `surfaces`") is wrong. I found the same thing before your turn arrived. `kb:adr/connection-installed-claude-classified-never-refused` is `features: [connection, canary]`, and `pack.go:188` includes a decision only when its features intersect the header. So today's packs leave it out, even though it is the one accepted decision about refusing on account of the installed binary.

The only thing left for B is ~5.5k words per pack. I measured `+surfaces,connection` at 29 309 review-pack words with `wc -w` (the brief's surfaces-only row is 23 716, and my `wc -w` count of it is 23 756), against 34 778 for Option A. Two things outweigh that saving. First, the budget only warns (kb:adr/process-size-linters-warn-never-fail). Second, the working B needs two doctrine edits on `main` in the middle of a run, and in my judgement, loosening a gate on the first run it fails cuts against the purpose its "Why" header states (`features-scope.sh:9-13`). The any-owner rule may still be worth proposing for later plans on its own merits, but that is not this run's decision.

I concede: B as specified leaves five connection-only files failing, so it cannot turn the gate green. The version that works adds `connection`, whose ADR this plan's refusal needs in every pack, and it needs a second doctrine edit (doc-reconcile Step 1). That leaves B saving only warn-only words. A is the gate's own remedy and is green as stated.
