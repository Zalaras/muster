# Web Implementation: Maintainability Cleanup — FW-Z web residue

**Plan**: maintainability-cleanup
**Mode**: fix (FW-Z residue wave)
**Scope**: web/src (non-test) only, per team-lead assignment. Daemon-side rows in
`review.maintainability.fixwave.md` (Majors 1-4, Minors 1-6, the Notes) are FW-Z-daemon's.

## Changes

| File | Action | What and Why |
|------|--------|--------------|
| `web/src/sessions/paths.ts` | created | `basename` moved here from `reader/paths.ts` — Minor 7: the move to `reader/paths.ts` had created a `sessions/` → `reader/` edge, contradicting kb:diagram/web-components's "sessions/ is the shared derivation layer below reader/". `sessions/` may depend on nothing in `reader/`. |
| `web/src/reader/paths.ts` | edited | Keeps only `loadingText`; imports `basename` from `../sessions/paths`. |
| `web/src/reader/paths.test.ts` | edited (import only) | Split the import: `basename` from `../sessions/paths`, `loadingText` from `./paths` — my own refactor invalidated the old single import line (allowed exception). Assertions untouched. |
| `web/src/sessions/card.ts` | edited | Imports `basename` from `./paths`. Added `PaneState` type (moved from `render/dead.ts`) and `deadSurfaceText(session, pane, now)`, the one composer for every dead-surface string (Minor 11). |
| `web/src/render/dead.ts` | edited | `renderDeadSurface` now calls `deadSurfaceText` once and only assigns the three fields; removed the local `PaneState` type and the inline endbar/cap composition. |
| `web/src/render/dead.test.ts` | edited (import only) | `PaneState` now imported from `../sessions/card` alongside `deadEndbarText` (refactor-invalidated import, allowed exception). |
| `web/src/features/actions.ts`, `features/tiles.ts`, `features/focus.ts`, `render/tiles.ts` | edited | Re-pointed `PaneState` type imports from `../render/dead` to `../sessions/card` (its new home); no behaviour change. |
| `web/src/main.ts` | edited | Minor 8: header now says only `tiles`/`focus` take forward-reference thunks; `surfaces`/`reader` take real values (verified against their own headers). |
| `web/src/protocol/decode.ts` | edited | Minor 9: `isNumber` un-exported (module-private; only `asNumber` in this file used it). Also dropped the "every wire parser hand-wrote these shapes" history narration from the header. |
| `web/src/features/focus.ts` | edited | Minor 9: removed `FocusHandle.nameEl` (dead export — `rename.ts` no longer reads it, `rg` confirms no other reader). |
| `web/src/api/prefs.ts` | edited | Minor 9 (d-m7 residue): `putPrefs`'s doc now states it stays exported for `prefs.test.ts`/`http.test.ts`'s direct coverage; every production caller goes through `sendPrefsPatch`. |
| `web/src/features/actionscopy.ts`, `features/actions.ts` | edited | Minor 10: dropped the unread `_now: Date` parameter from `endDialogBody`/`removeDialogBody`; call sites in `actions.ts` no longer pass `new Date()`. |
| `web/src/render/focusview.ts`, `features/focus.ts` | edited | Minor 12: `renderFocusMain` no longer writes the main slot's `hidden` (dropped `FocusMainElements.slotEl`) — every `renderView` branch already calls `setMainSlotHidden` explicitly regardless of `hasSessions`, so the old write was always immediately overwritten; `setMainSlotHidden` is now the slot's one writer. Also reworded the sizenote NBSP doc to state the current fact plainly (review.work.cycle2 note). |
| `web/src/protocol/hello.ts`, `prefs.ts`, `session.ts`, `theme.ts`, `update.ts`, `usage.ts`, `messages.ts` | edited | Minor 13/d-m11: dropped "split out of the former protocol.ts" (protocol.ts doesn't exist) from all 7 headers; reworded to state the current per-concept-module shape. |
| `web/src/protocol/usage.ts` | edited | Dropped "(down from 33 before the shared decoders absorbed …)" — a before/after narration — from the cognitive-complexity doc; kept the current-state reason. |
| `web/src/api/http.ts`, `web/src/dom.ts` | edited | Dropped "this used to be 8 copy-pasted…" / "was hand-rolled in … and again in …" history narration; restated as current facts (still names the real current call sites). |
| `web/src/features/update.ts` | edited | e-m11: fixed a wrong location citation — `CheckState`'s doc comment is in `features/updateview.ts`, not `render/update.ts`. |
| `web/src/render/update.ts` | edited | e-m11: added the missing doc comment on `UpdateSectionElements.toggle` that the `renderUpdateSection` doc pointed to ("see the field's own doc comment") but which didn't exist. Also dropped 2 "Testable UI Elements" plan-section citations (`checkBtn` doc, `toggle.checked` doc unaffected). |
| `web/src/terminal/overlay.ts`, `terminal/pane.ts`, `render/dead.ts`, `render/reader.ts`, `render/sessions.ts`, `render/tiles.ts`, `render/surfaceseg.ts` (×2) | edited | e-m11: removed the remaining 8 "Testable UI Elements"/"UI Specifications" plan-section citations found by `rg 'UI Specifications\|Testable UI Elements' web/src -g '!*.test.ts'` (9 hits before, 0 after outside `style.css`, which is untouched/out of scope — not in this wave's diff). Kept the underlying fact each comment stated, dropped only the document-section name. |
| `web/src/features/views.ts` | edited | Dropped "(main.ts formerly registered this second half itself)" — history narration. |
| `web/src/features/rename.ts` | edited | Dropped "the same way a tile already did before this module owned the mainhead's too" — history narration. |
| `web/src/features/tiles.ts`, `features/focus.ts`, `features/actions.ts` | edited | Deduplicated "never reached through features/actions.ts, which carries no dead-surface API" — stated fully once, in `actions.ts`'s header (the canonical "one owner" location); `tiles.ts`'s interface doc and impl doc, and `focus.ts`'s interface doc, now point back to it instead of repeating it (`tiles.ts` previously repeated it twice in the same file). |
| `web/src/features/actions.ts` | edited | Dropped "`render/dead.ts` used to also own this fetch trigger" — history narration on `loadPane`'s doc; restated as a current-state "why here, not there". |
| `web/src/render/sessions.ts` | edited | Dropped "unconverted" (pending-work framing) from the `CardOptions` doc's reference to `render/mainhead.ts`. |
| `web/src/features/surfaces.ts` | edited | Reworded "the Protocol Contract's `/ws` broadcast" → "docs/protocol.md's `/ws` broadcast" (plan-section-style citation; `Protocol Contract` is a plan-doc heading name). |
| `web/src/features/focus.ts` | edited | review.work.cycle2 Note 1 follow-up: restored the dropped "must not let a blur-driven commit through … kb:adr/views-active-segment-click-commits-rename" reason on the `cancelRenames`/`status` subscriptions (found via `git log -S"must not let" -- web/src`, commit 603685f dropped it when the subscriptions moved from `features/rename.ts` into `features/focus.ts`). |
| `web/src/reader/CLAUDE.md`, `web/src/sessions/CLAUDE.md` | edited | Updated the `Owns` prose for the `basename` move (Minor 7). |
| `docs/diagrams/web-components.md` | edited | `sessions/` module count 11 → 12 (added `paths.ts`). No edge changed — the diagram never drew a `sessions → reader` edge, so nothing to add/remove there; my fix just makes the code match what it already claimed. |
| `docs/features/rail/spec.md` | edited | Added `web/src/sessions/paths*.ts` to the `rail` feature's web glob (it sat beside `sessions/format*.ts`/`sessions/card*.ts`, both already owned by `rail`) — `make check-kb` failed with "owned by no feature" until this was added. |
| `.claude/rules/rail.md`, `docs/features/rail/INDEX.md` | regenerated | `make gen-kb` output from the spec.md glob change above. |

## Decisions

- `focusview.ts`'s NBSP: verified the byte is still `c2 a0` (U+00A0) after my comment-only edit (`sed -n '43p' render/focusview.ts \| xxd`), matching review.work.cycle2's Note 1. No code change; comment now states plainly that the reserving branch writes the NBSP and why (flex-item zero-height collapse), without narrating that it used to be an ASCII space.
- `render/mainhead.ts:59-60` ("Required, not optional — the one production caller always passes it") — verified as a **false positive**: `rg 'renderMainhead\('` finds exactly one call site (`features/focus.ts:205`), so the comment is true and isn't narrating history. Left unchanged.
- `web/src/style.css`'s many `REQ-N`/`plan`/`review` citations (60+, via the full-tree sweep) are **not this wave's residue**: `git diff 6db870d..HEAD -- web/src/style.css` shows only 3 hunks changed, all of which already had their citations removed in a prior cycle. The remaining hits predate `6db870d` and are outside this diff's scope; left untouched.
- Verified false positives from the regex sweep restricted to this wave's diff files (`git diff --name-only 6db870d..HEAD -- web/src ':!*.test.ts' ':!*.css'`, 66 files): every remaining `plan `/`review`/`split out of` hit is either (a) `review-Critical`, the established invariant-severity term used in every `CLAUDE.md`, (b) `plan` meaning Claude Code's plan-mode feature (a wire concept, `kb:anchor/ws.session`), or (c) `split out of <X>`/`Split out of <function>` where `X` is a file or function that still exists today (a legitimate present-tense structural cross-reference, not history narration of something removed). Listed per-line in the sweep transcript; none required a change.
- design: `sessions/paths.ts` is a new one-function module, sized and shaped like its sibling `reader/paths.ts` was before the move (a pure derivation, Vitest-tested, no DOM) — `rg` for an existing shared-utility home before adding it: `rg -n '^import' web/src -g '!*.test.ts'` showed no directory already sitting below both `sessions/` and `reader/` other than `sessions/` itself (which is explicitly documented as "the shared derivation layer below reader/"), so `sessions/` is where it belongs rather than a new top-level module.
- design: `sessions/card.ts`'s new `deadSurfaceText` composes what were three separate compositions (`sessions/card.ts`'s `deadEndbarText`/`deadCapPrefix` plus `render/dead.ts`'s inline captured-suffix/missing/loading text) into one function, per `render/CLAUDE.md`'s "a builder here takes the computed value, never the raw data" — `deadEndbarText`/`deadCapPrefix` stay exported (and tested directly by `card.test.ts`), `deadSurfaceText` is the one thing `render/dead.ts` now calls.
- deviation: none — every change here implements an existing review finding or restores dropped-in-a-prior-cycle intent; nothing here changes the plan's protocol contract or UI contract.
- doc-delta: none — no user-facing behaviour changed. `docs/diagrams/web-components.md`'s module count is a code-shape fact, not a spec/protocol claim, and is fixed above alongside the code change that caused the drift.

## Verification measured (not asserted from the diff)

- **Minor 7 (cycle):** `rg '^import' web/src -g '!*.test.ts'` re-run after the move — the only `sessions/` → `reader/` import was `sessions/card.ts`'s `basename` (now gone); `reader/freshness.ts`'s `ageAgo` import from `sessions/format` is the one direction the diagram draws. No remaining edge either way between the two directories besides `reader/` → `sessions/`.
- **Minor 12 DOM claim:** traced every branch of `features/focus.ts`'s `renderView` (the `!session` early return, the `dead` branch, `mountReader`, `mountTerminalSurface`) — each calls `setMainSlotHidden` explicitly regardless of `hasSessions`, so `renderFocusMain`'s old `slotEl.hidden` write was provably always overwritten in the same render pass before this change; removing it changes no observed DOM state. Confirmed with `make web-test`: the only new failure is `focusview.test.ts`'s own direct unit test of the old two-writer behaviour (see Handoff), not any other spec.
- **"Testable UI Elements" sweep:** `rg -n 'UI Specifications|Testable UI Elements' web/src -g '!*.test.ts'` — 9 hits before, 0 after outside `style.css` (which is untouched, pre-existing, out of this diff's scope per `git diff 6db870d..HEAD -- web/src/style.css`).

## Handoff

**Build status**: `npx tsc --noEmit` — **0 errors in non-test code**; 11 errors, all in exactly the two files below (sanctioned breakage). `npm run build` (`tsc --noEmit && vite build`) therefore also fails at the `tsc` step; confirmed the actual bundling step is clean by running `npx vite build` directly (succeeds, only the pre-existing >500kB chunk-size warning). `make web-lint`: clean (0 issues). `make check-kb`: 0 problems. `make refs`: 3138 references checked, 0 missing.

**Test files needing changes** (sanctioned breakage — not mine to edit per the impl-agent boundary):

1. `web/src/features/actionscopy.test.ts` — calls `endDialogBody(session, NOW)` / `removeDialogBody(session, NOW)` with a now-removed second argument, at lines 43, 51, 60, 66 (×2), 78, 91, 99 (8 call sites). TS errors only (`Expected 1 arguments, but got 2`); JS ignores the extra arg at runtime so `make web-test` still passes this file. `web-tests` should drop the second argument at each site.
2. `web/src/render/focusview.test.ts` — `renderFocusMain — toggles between the empty state and the terminal slot` builds `FocusMainElements` with a `slotEl` field that no longer exists on the type (TS errors at lines 10, 17, 24), and its first test (`"shows the empty state and hides the terminal slot when there are no sessions"`) fails at runtime (`make web-test`: 1 failed, 1764 passed) because `renderFocusMain` no longer touches `slotEl.hidden` — that's now `setMainSlotHidden`'s job alone (Minor 12). `web-tests` should drop the `slotEl` field and the two `expect(els.slotEl.hidden)...` assertions from this describe block; `setMainSlotHidden`'s own existing unit coverage (if any) or a new direct test covers that write instead.

No other REQ from the assigned residue list was left undone; every row in the team-lead's list (sessions↔reader cycle, main.ts header, dead exports, actionscopy `_now`, dead-surface second composer, `setMainSlotHidden`, comment sweep, focusview.ts NBSP) is addressed above.
